var scriptNameAna = "DCF"
var scriptName = "Developing Chai FrameWork"
var scriptVer = 0
var scriptVerSub = 1

var scriptVerFull = "v" + to_string(scriptVer) + "." + to_string(scriptVerSub)

var scriptFullName = scriptNameAna + " - " + scriptName + " (" + scriptVerFull + ")"

print(scriptFullName)
print("")

try {
    runscript("sandbox.chai")
} catch(e) {
    print(e.what())
}
